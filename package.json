{
  "name": "1869",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "nodemon": "nodemon server.js",
    "sass": "sass --watch dev/scss:public/css",
    "dev": "concurrently --kill-others \"npm run nodemon\" \"npm run sass\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0",
    "sass": "^1.74.1"
  }
}
